import { ELEMENTAL } from "../core/symbols";

export const SERVER_ELEMENT_SYMBOL = Symbol.for(`${ELEMENTAL}.server.element`);

export const SERVER_SHADOW_ROOT_SYMBOL = Symbol.for(`${ELEMENTAL}.server.shadow-root`);
