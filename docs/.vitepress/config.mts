import { defineConfig } from "vitepress";

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "Elemental",
  description: "Minimal, reactive, and functional web components.",
  themeConfig: {
    // https://vitepress.dev/reference/default-theme-config
    nav: [],

    sidebar: [],

    socialLinks: [{ icon: "github", link: "https://github.com/maverick-js/elemental" }],
  },
});
